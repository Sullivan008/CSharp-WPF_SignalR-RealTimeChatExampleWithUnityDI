<presenter:Presenter x:Class="App.Client.Wpf.Modules.Chat.Presenters.Chat.UserControls.ChatPresenter"
                     x:Name="ChatPresenterElement"
                     xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                     xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                     xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                     xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                     xmlns:behaviors="http://schemas.microsoft.com/xaml/behaviors"
                     xmlns:presenter="clr-namespace:SullyTech.Wpf.Controls.Presenter.Core.UserControls.Presenter;assembly=SullyTech.Wpf.Controls.Presenter.Core"
                     mc:Ignorable="d" 
                     d:DesignHeight="450" d:DesignWidth="800">

    <behaviors:Interaction.Triggers>
        <behaviors:EventTrigger EventName="Loaded">
            <behaviors:InvokeCommandAction Command="{Binding ElementName=ChatPresenterElement, Path=DataContext.LoadedCommand}"
                                           CommandParameter="{Binding ElementName=ChatPresenterElement}"/>
        </behaviors:EventTrigger>
    </behaviors:Interaction.Triggers>

    <Grid>
        <TextBlock Text="Hello! It's a Chat Presenter!"></TextBlock>
    </Grid>
</presenter:Presenter>
