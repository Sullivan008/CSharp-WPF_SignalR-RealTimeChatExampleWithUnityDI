<window:DialogWindow x:Class="Application.Client.Windows.Implementations.ExceptionDialog.Window.ExceptionDialogWindow"
                     xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                     xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                     xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                     xmlns:behaviors="http://schemas.microsoft.com/xaml/behaviors"
                     xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                     xmlns:window="clr-namespace:Application.Client.Windows.DialogWindow.Window;assembly=Application.Client.Windows.DialogWindow"
                     mc:Ignorable="d"
                     ResizeMode="NoResize" SizeToContent="WidthAndHeight"
                     WindowStartupLocation="CenterScreen"
                     MinHeight="50" Height="50" MaxHeight="650"
                     MinWidth="700" Width="700" MaxWidth="700"
                     Title="{Binding WindowSettings.Title}">

    <behaviors:Interaction.Triggers>
        <behaviors:EventTrigger EventName="Closing">
            <behaviors:InvokeCommandAction Command="{Binding CloseWindowCommand}" CommandParameter="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type window:DialogWindow}}}"/>
        </behaviors:EventTrigger>
    </behaviors:Interaction.Triggers>

    <ContentControl Content="{Binding ContentPresenter}"/>
</window:DialogWindow>
